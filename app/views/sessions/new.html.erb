<div class="min-h-screen bg-gradient-to-br from-red-50 via-white to-green-50 flex items-center justify-center px-4">
  <!-- Login Card -->
  <div class="w-full max-w-md mx-auto" style="margin-top: -10vh;">
    <div class="bg-white rounded-2xl shadow-2xl p-8 border-4 border-red-200">
      <!-- Header with Santa -->
      <div class="text-center mb-6">
        <div class="text-6xl mb-3 animate-bounce">🎅</div>
        <h1 class="text-3xl font-bold text-red-600 mb-2">Secret Santa</h1>
        <p class="text-gray-600">Welcome back! Sign in to continue</p>
      </div>

      <!-- Decorative Divider -->
      <div class="flex items-center mb-6">
        <div class="flex-1 border-t-2 border-red-200"></div>
        <span class="px-3 text-2xl">🎄</span>
        <div class="flex-1 border-t-2 border-red-200"></div>
      </div>

      <%= form_with(url: session_path, method: :post, class: "space-y-5") do |form| %>
        <div>
          <%= form.label :username, "Username", class: "block text-sm font-medium text-gray-700 mb-2" %>
          <%= form.text_field :username, class: "w-full px-4 py-3 border-2 border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent transition-all", placeholder: "santa_claus", required: true, autocomplete: "off" %>
          <p class="text-xs text-gray-500 mt-1">Just enter your username - no password needed! 🎅</p>
        </div>

        <div class="pt-2">
          <%= form.submit "🎁 Sign In", class: "w-full bg-gradient-to-r from-red-600 to-red-700 text-white py-3 rounded-lg font-bold text-lg shadow-lg hover:from-red-700 hover:to-red-800 transition-all transform hover:scale-105 cursor-pointer" %>
        </div>
      <% end %>

      <!-- Decorative Divider -->
      <div class="flex items-center my-6">
        <div class="flex-1 border-t-2 border-gray-200"></div>
        <span class="px-3 text-xl">❄️</span>
        <div class="flex-1 border-t-2 border-gray-200"></div>
      </div>

      <div class="text-center">
        <p class="text-gray-600">
          Don't have an account?
          <%= link_to new_user_path, class: "text-red-600 font-bold hover:text-red-700 hover:underline transition-all" do %>
            Sign up 🎄
          <% end %>
        </p>
      </div>
    </div>

    <!-- Footer Message -->
    <div class="text-center mt-6">
      <p class="text-gray-500 text-sm">🎅 Making gift exchanges magical since 2025 ✨</p>
    </div>
  </div>
</div>
